{
  "//": "PostToolUse ESLint-on-edit was removed: this is a Python project (ADR-0001), the app has no JS/TS sources, and the factory's own .mjs check-scripts have no eslint config. Deterministic feedback comes from ruff (pre-commit) and pytest in the container.",
  "hooks": {}
}
